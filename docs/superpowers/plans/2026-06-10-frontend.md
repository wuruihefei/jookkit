# JookKit 前端 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 JookKit 的 C++/Qt 前端——拉起 Java 后端子进程,通过本地 HTTP 发 `{funcId,...}` 请求,提供连接管理、对象树、SQL 编辑执行、数据网格编辑、表结构查看的桌面 UI。

**Architecture:** Qt5 Widgets 应用。**先实现可独立测试的后端通信基础设施**(`ConnData` 模型、`BackendProcess` 子进程管理、`BackendClient` HTTP 客户端),再在其上**补齐 jookdb 残缺的 UI 组件**(复用 `MainWindow`/`ContentWidget`/`MyTreeWidget` 等已有结构,适配 `QueryForm`/`TableDataForm`/`TableForm`/`ConnDialog`)。

**Tech Stack:** C++17,Qt5(core/gui/widgets/network/testlib),qmake 构建。

**前置依赖:** 后端计划(`2026-06-10-backend.md`)已完成,`jookkit-backend-all.jar` 可构建。集成测试需要该 jar 存在。

---

## File Structure

```
frontend/
  jookkit.pro                          # qmake 工程(app)
  src/
    main.cpp                           # 入口
    backend/
      ConnData.h / .cpp                # 连接配置模型 + toRequestJson()
      BackendProcess.h / .cpp          # QProcess 拉起 jar,捕获 JOOKKIT_PORT
      BackendClient.h / .cpp           # QNetworkAccessManager POST /rpc
      FuncId.h                         # 与后端共享的操作码常量(C++ 侧副本)
    ui/                                # Phase B:补齐的 UI(适配 jookdb 残缺源码)
      MainWindow.* ContentWidget.* ... # 复用 jookdb 结构
  tests/
    tst_conndata.cpp                   # ConnData 单测
    tst_backend_integration.cpp        # BackendProcess + BackendClient 集成测试
  test.pro                             # qmake 测试工程
```

**Phase A(Task 1–6)**:基础设施,完整 TDD,可在无 UI 情况下跑通"启动后端 → 连接 → 查询"。
**Phase B(Task 7–11)**:UI 补齐,需先读 jookdb 残缺源码再适配。

---

## Phase A:后端通信基础设施

## Task 1: qmake 工程脚手架 + QtTest 骨架

**Files:**
- Create: `frontend/jookkit.pro`
- Create: `frontend/src/main.cpp`
- Create: `frontend/test.pro`
- Create: `frontend/tests/tst_smoke.cpp`

- [ ] **Step 1: 写主工程 jookkit.pro**

Create `frontend/jookkit.pro`:
```pro
QT += core gui widgets network
greaterThan(QT_MAJOR_VERSION, 4): QT += widgets
CONFIG += c++17
TARGET = jookkit
TEMPLATE = app

SOURCES += \
    src/main.cpp \
    src/backend/ConnData.cpp \
    src/backend/BackendProcess.cpp \
    src/backend/BackendClient.cpp

HEADERS += \
    src/backend/ConnData.h \
    src/backend/BackendProcess.h \
    src/backend/BackendClient.h \
    src/backend/FuncId.h
```

- [ ] **Step 2: 写最小 main.cpp**

Create `frontend/src/main.cpp`:
```cpp
#include <QApplication>
#include <QLabel>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    QLabel label("JookKit placeholder");
    label.show();
    return app.exec();
}
```

- [ ] **Step 3: 写测试工程 test.pro**

Create `frontend/test.pro`:
```pro
QT += core network testlib
CONFIG += c++17 console testcase
TEMPLATE = app
TARGET = tst_jookkit

INCLUDEPATH += src

SOURCES += \
    tests/tst_smoke.cpp \
    src/backend/ConnData.cpp \
    src/backend/BackendProcess.cpp \
    src/backend/BackendClient.cpp

HEADERS += \
    src/backend/ConnData.h \
    src/backend/BackendProcess.h \
    src/backend/BackendClient.h \
    src/backend/FuncId.h
```

- [ ] **Step 4: 写 smoke 测试**

Create `frontend/tests/tst_smoke.cpp`:
```cpp
#include <QtTest>

class TstSmoke : public QObject {
    Q_OBJECT
private slots:
    void buildWorks() { QCOMPARE(2 + 2, 4); }
};

QTEST_MAIN(TstSmoke)
#include "tst_smoke.moc"
```

注:为让 test.pro 能编译,先建空的源/头文件占位(下一步起逐个填实),或暂时从 test.pro 的 SOURCES/HEADERS 移除尚未创建的文件,仅保留 tst_smoke。**采用后者**:本步 test.pro 的 SOURCES 仅含 `tests/tst_smoke.cpp`,HEADERS 留空;Task 2 起再把对应文件加回。

- [ ] **Step 5: 构建并运行 smoke 测试**

Run: `cd frontend && qmake test.pro && make && ./tst_jookkit`
Expected: `Totals: 1 passed, 0 failed`。

- [ ] **Step 6: Commit**

```bash
cd ~/dev/project/jookkit
git add frontend/jookkit.pro frontend/test.pro frontend/src/main.cpp frontend/tests/tst_smoke.cpp
git commit -m "build(frontend): qmake scaffold + qttest smoke"
```

---

## Task 2: FuncId 常量 + ConnData 模型

**Files:**
- Create: `frontend/src/backend/FuncId.h`
- Create: `frontend/src/backend/ConnData.h`
- Create: `frontend/src/backend/ConnData.cpp`
- Create: `frontend/tests/tst_conndata.cpp`
- Modify: `frontend/test.pro`(加回 ConnData 与新测试)

- [ ] **Step 1: 写 FuncId.h(与后端 FuncId.java 数值一一对应)**

Create `frontend/src/backend/FuncId.h`:
```cpp
#ifndef JOOKKIT_FUNCID_H
#define JOOKKIT_FUNCID_H

namespace FuncId {
    constexpr int TEST_CONNECTION  = 1001;
    constexpr int OPEN_CONNECTION  = 1002;
    constexpr int CLOSE_CONNECTION = 1003;
    constexpr int LIST_DATABASES   = 2001;
    constexpr int LIST_TABLES      = 2002;
    constexpr int DESCRIBE_TABLE   = 2003;
    constexpr int EXEC_SQL         = 3001;
    constexpr int INSERT_ROW       = 4001;
    constexpr int UPDATE_ROW       = 4002;
    constexpr int DELETE_ROW       = 4003;
}

#endif
```

- [ ] **Step 2: 写失败测试**

Create `frontend/tests/tst_conndata.cpp`:
```cpp
#include <QtTest>
#include <QJsonObject>
#include "backend/ConnData.h"
#include "backend/FuncId.h"

class TstConnData : public QObject {
    Q_OBJECT
private slots:
    void sqliteOpenRequest() {
        ConnData c;
        c.connId = "c1";
        c.type = "sqlite";
        c.file = "/tmp/x.db";
        QJsonObject req = c.toOpenRequest();
        QCOMPARE(req.value("funcId").toInt(), FuncId::OPEN_CONNECTION);
        QCOMPARE(req.value("connId").toString(), QString("c1"));
        QCOMPARE(req.value("type").toString(), QString("sqlite"));
        QCOMPARE(req.value("file").toString(), QString("/tmp/x.db"));
    }

    void mysqlOpenRequest() {
        ConnData c;
        c.connId = "m1";
        c.type = "mysql";
        c.host = "localhost";
        c.port = 3306;
        c.user = "root";
        c.database = "test";
        QJsonObject req = c.toOpenRequest();
        QCOMPARE(req.value("host").toString(), QString("localhost"));
        QCOMPARE(req.value("port").toInt(), 3306);
        QCOMPARE(req.value("user").toString(), QString("root"));
        QCOMPARE(req.value("database").toString(), QString("test"));
    }
};

QTEST_MAIN(TstConnData)
#include "tst_conndata.moc"
```

- [ ] **Step 3: 改 test.pro 指向新测试**

把 `frontend/test.pro` 的 SOURCES 改为(每个测试是独立 QTEST_MAIN,单独跑;此处切换到 conndata 测试):
```pro
SOURCES += \
    tests/tst_conndata.cpp \
    src/backend/ConnData.cpp
HEADERS += \
    src/backend/ConnData.h \
    src/backend/FuncId.h
```

- [ ] **Step 4: 运行验证失败**

Run: `cd frontend && qmake test.pro && make`
Expected: 编译失败(ConnData.h 不存在)。

- [ ] **Step 5: 写 ConnData.h**

Create `frontend/src/backend/ConnData.h`:
```cpp
#ifndef JOOKKIT_CONNDATA_H
#define JOOKKIT_CONNDATA_H

#include <QString>
#include <QJsonObject>

class ConnData {
public:
    QString connId;
    QString type;       // "mysql" | "sqlite"
    QString host;
    int port = 0;
    QString user;
    QString password;
    QString database;
    QString file;       // sqlite

    QJsonObject toOpenRequest() const;
};

#endif
```

- [ ] **Step 6: 写 ConnData.cpp**

Create `frontend/src/backend/ConnData.cpp`:
```cpp
#include "backend/ConnData.h"
#include "backend/FuncId.h"

QJsonObject ConnData::toOpenRequest() const {
    QJsonObject o;
    o.insert("funcId", FuncId::OPEN_CONNECTION);
    o.insert("connId", connId);
    o.insert("type", type);
    if (type == "sqlite") {
        o.insert("file", file);
    } else {
        o.insert("host", host);
        o.insert("port", port);
        o.insert("user", user);
        o.insert("password", password);
        o.insert("database", database);
    }
    return o;
}
```

- [ ] **Step 7: 运行测试验证通过**

Run: `cd frontend && qmake test.pro && make && ./tst_jookkit`
Expected: `2 passed, 0 failed`。

- [ ] **Step 8: Commit**

```bash
cd ~/dev/project/jookkit
git add frontend/src/backend/FuncId.h frontend/src/backend/ConnData.h frontend/src/backend/ConnData.cpp frontend/tests/tst_conndata.cpp frontend/test.pro
git commit -m "feat(frontend): funcid constants + ConnData model"
```

---

## Task 3: BackendClient(同步式 HTTP RPC)

`BackendClient` 封装 `QNetworkAccessManager`,提供阻塞式 `call(QJsonObject req) -> Result`(内部用 `QEventLoop` 等待),返回 `{ok, data, errorCode, errorMessage}`。先建客户端,Task 4 再建进程管理,Task 5 集成。

**Files:**
- Create: `frontend/src/backend/BackendClient.h`
- Create: `frontend/src/backend/BackendClient.cpp`

(本任务无独立单测——其行为依赖真实 HTTP,在 Task 5 集成测试中覆盖。本任务只保证编译通过。)

- [ ] **Step 1: 写 BackendClient.h**

Create `frontend/src/backend/BackendClient.h`:
```cpp
#ifndef JOOKKIT_BACKENDCLIENT_H
#define JOOKKIT_BACKENDCLIENT_H

#include <QObject>
#include <QJsonObject>
#include <QString>

class QNetworkAccessManager;

class BackendClient : public QObject {
    Q_OBJECT
public:
    struct Result {
        bool ok = false;
        QJsonObject data;
        QString errorCode;
        QString errorMessage;
        bool transportFailed = false;  // 网络层失败(非业务错误)
    };

    explicit BackendClient(QObject *parent = nullptr);
    void setPort(int port);

    /** 阻塞式调用 /rpc,超时返回 transportFailed。 */
    Result call(const QJsonObject &request, int timeoutMs = 30000);

private:
    QNetworkAccessManager *nam;
    int port_ = 0;
};

#endif
```

- [ ] **Step 2: 写 BackendClient.cpp**

Create `frontend/src/backend/BackendClient.cpp`:
```cpp
#include "backend/BackendClient.h"

#include <QNetworkAccessManager>
#include <QNetworkRequest>
#include <QNetworkReply>
#include <QJsonDocument>
#include <QEventLoop>
#include <QTimer>
#include <QUrl>

BackendClient::BackendClient(QObject *parent)
    : QObject(parent), nam(new QNetworkAccessManager(this)) {}

void BackendClient::setPort(int port) { port_ = port; }

BackendClient::Result BackendClient::call(const QJsonObject &request, int timeoutMs) {
    Result result;

    QUrl url(QString("http://127.0.0.1:%1/rpc").arg(port_));
    QNetworkRequest req(url);
    req.setHeader(QNetworkRequest::ContentTypeHeader, "application/json");

    QByteArray body = QJsonDocument(request).toJson(QJsonDocument::Compact);
    QNetworkReply *reply = nam->post(req, body);

    QEventLoop loop;
    QTimer timer;
    timer.setSingleShot(true);
    QObject::connect(&timer, &QTimer::timeout, &loop, &QEventLoop::quit);
    QObject::connect(reply, &QNetworkReply::finished, &loop, &QEventLoop::quit);
    timer.start(timeoutMs);
    loop.exec();

    if (!timer.isActive()) { // 超时
        result.transportFailed = true;
        result.errorCode = "TIMEOUT";
        result.errorMessage = "backend request timed out";
        reply->abort();
        reply->deleteLater();
        return result;
    }
    timer.stop();

    if (reply->error() != QNetworkReply::NoError) {
        result.transportFailed = true;
        result.errorCode = "TRANSPORT";
        result.errorMessage = reply->errorString();
        reply->deleteLater();
        return result;
    }

    QByteArray resp = reply->readAll();
    reply->deleteLater();

    QJsonObject obj = QJsonDocument::fromJson(resp).object();
    result.ok = obj.value("ok").toBool();
    if (result.ok) {
        result.data = obj.value("data").toObject();
    } else {
        QJsonObject err = obj.value("error").toObject();
        result.errorCode = err.value("code").toString();
        result.errorMessage = err.value("message").toString();
    }
    return result;
}
```

- [ ] **Step 3: 验证编译(临时把 BackendClient 加入 test.pro 编译)**

临时在 `frontend/test.pro` SOURCES 加 `src/backend/BackendClient.cpp`、HEADERS 加 `src/backend/BackendClient.h`,运行:
Run: `cd frontend && qmake test.pro && make`
Expected: 编译成功(测试仍只跑 conndata)。

- [ ] **Step 4: Commit**

```bash
cd ~/dev/project/jookkit
git add frontend/src/backend/BackendClient.h frontend/src/backend/BackendClient.cpp frontend/test.pro
git commit -m "feat(frontend): BackendClient blocking http rpc"
```

---

## Task 4: BackendProcess(拉起 jar,捕获端口)

`BackendProcess` 用 `QProcess` 启动 `java -jar <jar> --port 0`,从 stdout 读取 `JOOKKIT_PORT=<n>` 完成握手,暴露 `port()` 与 `start()/stop()`,析构时杀子进程。

**Files:**
- Create: `frontend/src/backend/BackendProcess.h`
- Create: `frontend/src/backend/BackendProcess.cpp`

- [ ] **Step 1: 写 BackendProcess.h**

Create `frontend/src/backend/BackendProcess.h`:
```cpp
#ifndef JOOKKIT_BACKENDPROCESS_H
#define JOOKKIT_BACKENDPROCESS_H

#include <QObject>
#include <QProcess>
#include <QString>

class BackendProcess : public QObject {
    Q_OBJECT
public:
    explicit BackendProcess(const QString &jarPath, QObject *parent = nullptr);
    ~BackendProcess() override;

    /** 启动并阻塞等待握手(读到 JOOKKIT_PORT)。成功返回 true。 */
    bool start(int handshakeTimeoutMs = 15000);
    void stop();
    int port() const { return port_; }
    bool isRunning() const;

private:
    QString jarPath_;
    QProcess *proc_;
    int port_ = 0;
};

#endif
```

- [ ] **Step 2: 写 BackendProcess.cpp**

Create `frontend/src/backend/BackendProcess.cpp`:
```cpp
#include "backend/BackendProcess.h"

#include <QElapsedTimer>
#include <QRegularExpression>
#include <QCoreApplication>

BackendProcess::BackendProcess(const QString &jarPath, QObject *parent)
    : QObject(parent), jarPath_(jarPath), proc_(new QProcess(this)) {
    proc_->setProcessChannelMode(QProcess::MergedChannels);
}

BackendProcess::~BackendProcess() { stop(); }

bool BackendProcess::start(int handshakeTimeoutMs) {
    proc_->start("java", {"-jar", jarPath_, "--port", "0"});
    if (!proc_->waitForStarted(5000)) return false;

    QElapsedTimer timer;
    timer.start();
    QByteArray buf;
    QRegularExpression re("JOOKKIT_PORT=(\\d+)");
    while (timer.elapsed() < handshakeTimeoutMs) {
        if (proc_->waitForReadyRead(500)) {
            buf += proc_->readAll();
            auto m = re.match(QString::fromUtf8(buf));
            if (m.hasMatch()) {
                port_ = m.captured(1).toInt();
                return port_ > 0;
            }
        }
        if (proc_->state() == QProcess::NotRunning) return false;
    }
    return false;
}

void BackendProcess::stop() {
    if (proc_->state() != QProcess::NotRunning) {
        proc_->terminate();
        if (!proc_->waitForFinished(3000)) proc_->kill();
    }
}

bool BackendProcess::isRunning() const {
    return proc_->state() != QProcess::NotRunning;
}
```

- [ ] **Step 3: 验证编译**

临时在 `frontend/test.pro` 加入 `src/backend/BackendProcess.cpp` / `.h`,运行:
Run: `cd frontend && qmake test.pro && make`
Expected: 编译成功。

- [ ] **Step 4: Commit**

```bash
cd ~/dev/project/jookkit
git add frontend/src/backend/BackendProcess.h frontend/src/backend/BackendProcess.cpp frontend/test.pro
git commit -m "feat(frontend): BackendProcess launch + port handshake"
```

---

## Task 5: 集成测试(进程 + 客户端跑通真实链路)

需要后端 jar。集成测试通过环境变量 `JOOKKIT_JAR` 指向 `jookkit-backend-all.jar`;未设置则跳过(QSKIP)。

**Files:**
- Create: `frontend/tests/tst_backend_integration.cpp`
- Create: `frontend/test_integration.pro`

- [ ] **Step 1: 写集成测试**

Create `frontend/tests/tst_backend_integration.cpp`:
```cpp
#include <QtTest>
#include <QProcessEnvironment>
#include <QJsonObject>
#include "backend/BackendProcess.h"
#include "backend/BackendClient.h"
#include "backend/ConnData.h"
#include "backend/FuncId.h"

class TstBackendIntegration : public QObject {
    Q_OBJECT
    QString jar;
private slots:
    void initTestCase() {
        jar = QProcessEnvironment::systemEnvironment().value("JOOKKIT_JAR");
        if (jar.isEmpty()) QSKIP("set JOOKKIT_JAR to run integration test");
    }

    void openAndQuerySqlite() {
        BackendProcess proc(jar);
        QVERIFY2(proc.start(), "backend failed to start / handshake");
        QVERIFY(proc.port() > 0);

        BackendClient client;
        client.setPort(proc.port());

        ConnData c;
        c.connId = "c1"; c.type = "sqlite"; c.file = ":memory:";
        auto r1 = client.call(c.toOpenRequest());
        QVERIFY2(r1.ok, qPrintable(r1.errorMessage));

        QJsonObject exec;
        exec.insert("funcId", FuncId::EXEC_SQL);
        exec.insert("connId", "c1");
        exec.insert("sql", "select 99 as n");
        auto r2 = client.call(exec);
        QVERIFY2(r2.ok, qPrintable(r2.errorMessage));
        int v = r2.data.value("rows").toArray()
                  .at(0).toArray().at(0).toInt();
        QCOMPARE(v, 99);
    }

    void sqlErrorSurfaces() {
        BackendProcess proc(jar);
        QVERIFY(proc.start());
        BackendClient client;
        client.setPort(proc.port());
        ConnData c; c.connId = "c1"; c.type = "sqlite"; c.file = ":memory:";
        client.call(c.toOpenRequest());

        QJsonObject exec;
        exec.insert("funcId", FuncId::EXEC_SQL);
        exec.insert("connId", "c1");
        exec.insert("sql", "select * from nope");
        auto r = client.call(exec);
        QVERIFY(!r.ok);
        QCOMPARE(r.errorCode, QString("SQL_ERROR"));
    }
};

QTEST_MAIN(TstBackendIntegration)
#include "tst_backend_integration.moc"
```

注:`r2.data.value("rows").toArray()` 需包含 `<QJsonArray>`;在文件顶部加 `#include <QJsonArray>`。

- [ ] **Step 2: 写集成测试工程**

Create `frontend/test_integration.pro`:
```pro
QT += core network testlib
CONFIG += c++17 console testcase
TEMPLATE = app
TARGET = tst_integration
INCLUDEPATH += src

SOURCES += \
    tests/tst_backend_integration.cpp \
    src/backend/ConnData.cpp \
    src/backend/BackendProcess.cpp \
    src/backend/BackendClient.cpp
HEADERS += \
    src/backend/ConnData.h \
    src/backend/BackendProcess.h \
    src/backend/BackendClient.h \
    src/backend/FuncId.h
```

- [ ] **Step 3: 构建后端 jar(若尚未构建)**

Run: `cd ~/dev/project/jookkit/backend && gradle shadowJar`
Expected: `backend/build/libs/jookkit-backend-all.jar` 存在。

- [ ] **Step 4: 运行集成测试**

Run:
```bash
cd ~/dev/project/jookkit/frontend
qmake test_integration.pro && make
JOOKKIT_JAR=~/dev/project/jookkit/backend/build/libs/jookkit-backend-all.jar ./tst_integration
```
Expected: `2 passed, 0 failed`(真实拉起后端、连接、查询、错误回传全通)。

- [ ] **Step 5: Commit**

```bash
cd ~/dev/project/jookkit
git add frontend/tests/tst_backend_integration.cpp frontend/test_integration.pro
git commit -m "test(frontend): end-to-end backend process + client integration"
```

---

## Task 6: 最小可用主窗口(连接 → 查询 → 表格)

不依赖 jookdb 残缺源码,先用最简 Qt 控件把"基础设施"串成一个能跑的窗口,作为 Phase B 替换前的可用骨架。

**Files:**
- Create: `frontend/src/ui/MinimalWindow.h`
- Create: `frontend/src/ui/MinimalWindow.cpp`
- Modify: `frontend/src/main.cpp`
- Modify: `frontend/jookkit.pro`

- [ ] **Step 1: 写 MinimalWindow.h**

Create `frontend/src/ui/MinimalWindow.h`:
```cpp
#ifndef JOOKKIT_MINIMALWINDOW_H
#define JOOKKIT_MINIMALWINDOW_H

#include <QMainWindow>
#include "backend/BackendProcess.h"
#include "backend/BackendClient.h"

class QPlainTextEdit;
class QTableWidget;
class QListWidget;

class MinimalWindow : public QMainWindow {
    Q_OBJECT
public:
    explicit MinimalWindow(const QString &jarPath, QWidget *parent = nullptr);

private slots:
    void openSqlite();
    void runSql();
    void refreshTables();

private:
    BackendProcess *proc_;
    BackendClient *client_;
    QListWidget *tableList_;
    QPlainTextEdit *editor_;
    QTableWidget *grid_;
    bool connected_ = false;
};

#endif
```

- [ ] **Step 2: 写 MinimalWindow.cpp**

Create `frontend/src/ui/MinimalWindow.cpp`:
```cpp
#include "ui/MinimalWindow.h"
#include "backend/ConnData.h"
#include "backend/FuncId.h"

#include <QPlainTextEdit>
#include <QTableWidget>
#include <QListWidget>
#include <QSplitter>
#include <QToolBar>
#include <QAction>
#include <QFileDialog>
#include <QMessageBox>
#include <QJsonArray>
#include <QVBoxLayout>
#include <QWidget>

MinimalWindow::MinimalWindow(const QString &jarPath, QWidget *parent)
    : QMainWindow(parent),
      proc_(new BackendProcess(jarPath, this)),
      client_(new BackendClient(this)) {

    if (!proc_->start()) {
        QMessageBox::critical(this, "JookKit", "后端启动失败");
    } else {
        client_->setPort(proc_->port());
    }

    auto *tb = addToolBar("main");
    tb->addAction("打开 SQLite...", this, &MinimalWindow::openSqlite);
    tb->addAction("运行 SQL", this, &MinimalWindow::runSql);

    tableList_ = new QListWidget;
    editor_ = new QPlainTextEdit;
    grid_ = new QTableWidget;

    auto *right = new QWidget;
    auto *rl = new QVBoxLayout(right);
    rl->addWidget(editor_, 1);
    rl->addWidget(grid_, 2);

    auto *split = new QSplitter;
    split->addWidget(tableList_);
    split->addWidget(right);
    split->setStretchFactor(1, 1);
    setCentralWidget(split);
    resize(900, 600);
}

void MinimalWindow::openSqlite() {
    QString file = QFileDialog::getOpenFileName(this, "选择 SQLite 文件");
    if (file.isEmpty()) return;
    ConnData c;
    c.connId = "c1"; c.type = "sqlite"; c.file = file;
    auto r = client_->call(c.toOpenRequest());
    if (!r.ok) { QMessageBox::warning(this, "连接失败", r.errorMessage); return; }
    connected_ = true;
    refreshTables();
}

void MinimalWindow::refreshTables() {
    if (!connected_) return;
    QJsonObject req;
    req.insert("funcId", FuncId::LIST_TABLES);
    req.insert("connId", "c1");
    req.insert("db", "main");
    auto r = client_->call(req);
    tableList_->clear();
    if (!r.ok) return;
    for (const auto &t : r.data.value("tables").toArray())
        tableList_->addItem(t.toString());
}

void MinimalWindow::runSql() {
    if (!connected_) { QMessageBox::information(this, "提示", "请先打开连接"); return; }
    QJsonObject req;
    req.insert("funcId", FuncId::EXEC_SQL);
    req.insert("connId", "c1");
    req.insert("sql", editor_->toPlainText());
    auto r = client_->call(req);
    if (!r.ok) { QMessageBox::warning(this, "SQL 错误", r.errorMessage); return; }

    QJsonObject data = r.data;
    if (!data.value("isQuery").toBool()) {
        QMessageBox::information(this, "完成",
            QString("影响行数: %1").arg(data.value("affected").toInt()));
        return;
    }
    QJsonArray cols = data.value("columns").toArray();
    QJsonArray rows = data.value("rows").toArray();
    grid_->clear();
    grid_->setColumnCount(cols.size());
    QStringList headers;
    for (const auto &c : cols) headers << c.toObject().value("name").toString();
    grid_->setHorizontalHeaderLabels(headers);
    grid_->setRowCount(rows.size());
    for (int i = 0; i < rows.size(); ++i) {
        QJsonArray row = rows.at(i).toArray();
        for (int j = 0; j < row.size(); ++j)
            grid_->setItem(i, j, new QTableWidgetItem(
                row.at(j).isNull() ? "(null)" : row.at(j).toVariant().toString()));
    }
}
```

- [ ] **Step 3: 改 main.cpp 启动 MinimalWindow**

Replace `frontend/src/main.cpp`:
```cpp
#include <QApplication>
#include <QStandardPaths>
#include <QFileInfo>
#include "ui/MinimalWindow.h"

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    // jar 路径:优先环境变量,否则相对默认位置
    QString jar = qEnvironmentVariable("JOOKKIT_JAR",
        QCoreApplication::applicationDirPath()
        + "/../backend/build/libs/jookkit-backend-all.jar");
    MinimalWindow w(jar);
    w.show();
    return app.exec();
}
```

- [ ] **Step 4: 把 ui 文件加入 jookkit.pro**

Modify `frontend/jookkit.pro`,SOURCES 增 `src/ui/MinimalWindow.cpp`,HEADERS 增 `src/ui/MinimalWindow.h`,并加 `INCLUDEPATH += src`。

- [ ] **Step 5: 构建并手动冒烟**

Run: `cd frontend && qmake jookkit.pro && make`
然后:
```bash
JOOKKIT_JAR=~/dev/project/jookkit/backend/build/libs/jookkit-backend-all.jar ./jookkit
```
手动验证:工具栏"打开 SQLite"选一个 `.db`(或先用后端建表的库)→ 左侧出现表名 → 编辑器写 `select ...` → "运行 SQL" → 表格出结果。

- [ ] **Step 6: Commit**

```bash
cd ~/dev/project/jookkit
git add frontend/src/ui/MinimalWindow.h frontend/src/ui/MinimalWindow.cpp frontend/src/main.cpp frontend/jookkit.pro
git commit -m "feat(frontend): minimal usable window (open/list/query/grid)"
```

至此 **Phase A 完成**:前端能拉起后端、连接 SQLite、列表、执行 SQL 并展示结果。这是一个可运行、可演示的纵向切片。

---

## Phase B:补齐 jookdb 残缺 UI(适配既有源码)

> **执行须知:** Phase B 复用 jookdb 公开的 C++ UI 结构。每个任务**必须先读对应的 jookdb 残缺源文件**,理解其依赖(尤其是缺失的 `utils.h`/`ConnData` 用法、信号槽),再决定适配代码。因此下列任务给出的是**明确的适配步骤与边界**,具体代码在执行时读源后确定。这不是占位符——是"先读后写"的工程纪律。jookdb 源码位于 `~/dev/project/jookdb/cpp/`,只读参考,不修改。

通用约束:所有 DB 访问改为通过 `BackendClient::call()`,替换 jookdb 原先的 `Utils::postEsbWithNS`/`getResJson`;所有连接信息用本项目 `ConnData`,替换缺失的 jookdb `ConnData`。

## Task 7: 移植 ContentWidget + MyTreeWidget(对象树骨架)

**参考源:** `~/dev/project/jookdb/cpp/contentwidget.{h,cpp}`、`mytreewidget.{h,cpp}`、`leftwidgetform.{h,cpp}`

- [ ] **Step 1:** 读上述源文件,列出它们引用的缺失符号(如 `Utils::getConnData`、`ConnData`、`ParsedSql`、各 `*Form`),记录到任务笔记。
- [ ] **Step 2:** 把 `MyTreeWidget` 移植进 `src/ui/`,去掉对缺失类型的依赖,树节点数据改用本项目 `ConnData` + 后端 `LIST_DATABASES`/`LIST_TABLES` 填充。
- [ ] **Step 3:** 移植 `ContentWidget`(左树 + 右 `QTabWidget` 布局),先只接 MinimalWindow 已有的查询能力。
- [ ] **Step 4:** 用 MinimalWindow 的连接流程驱动树:连接成功后调用 `LIST_TABLES` 填充树;双击表名打开查询标签。
- [ ] **Step 5:** 手动冒烟:连接 → 树展开看到表 → 双击 → 右侧打开标签。
- [ ] **Step 6:** Commit:`feat(frontend): port content widget + object tree`

## Task 8: 移植 QueryForm + MyEdit + SqlLexer(SQL 编辑器)

**参考源:** `~/dev/project/jookdb/cpp/queryform.{h,cpp}`、`myedit.*`(`.cpp` 缺,需补)、`sqllexer.h`、`sqlstyle.*`

- [ ] **Step 1:** 读 `queryform.h`/`sqllexer.h`/`sqlstyle.*`,确认 `MyEdit` 的接口(头文件存在,实现缺失),列出需补齐的方法。
- [ ] **Step 2:** 补齐 `MyEdit`(基于 `QPlainTextEdit`,实现行号、当前行高亮等 `myedit.h` 声明的接口)。
- [ ] **Step 3:** 移植 `SqlLexer`(纯逻辑,几乎可直接用),为其写 QtTest 单测(语句切分、`nextSql`)。**此任务有真单测**:覆盖多语句切分、注释、分隔符。
- [ ] **Step 4:** 移植 `QueryForm`,把"运行"接到 `BackendClient` 的 `EXEC_SQL`;结果渲染复用/改造其结果表格;多语句用 `SqlLexer` 切分后逐条执行。
- [ ] **Step 5:** 把 `QueryForm` 接入 `ContentWidget` 的标签页,替换 Phase A 的简易编辑器。
- [ ] **Step 6:** 手动冒烟 + 运行 SqlLexer 单测。
- [ ] **Step 7:** Commit:`feat(frontend): port query editor (QueryForm + MyEdit + SqlLexer)`

## Task 9: 移植 TableDataForm(数据网格编辑)

**参考源:** jookdb 的数据编辑表单(`tabledataform.*`,在残缺源中,需确认存在性;不存在则基于 `ResultForm` 思路新建)+ 各 `*delegate`

- [ ] **Step 1:** 确认 jookdb 残缺源里数据编辑相关文件的存在与接口;读 `resultform.h`、相关 delegate。
- [ ] **Step 2:** 实现/移植数据网格:加载某表数据(`EXEC_SQL` 的 `select * from t limit N`),记录主键(来自 `DESCRIBE_TABLE` 的 `primaryKeys`)。
- [ ] **Step 3:** 就地编辑提交:改单元格 → 组 `UPDATE_ROW`(pk 定位);新增行 → `INSERT_ROW`;删行 → `DELETE_ROW`。
- [ ] **Step 4:** 手动冒烟:打开表数据 → 改一格 → 提交 → 重查确认落库。
- [ ] **Step 5:** Commit:`feat(frontend): table data grid editing`

## Task 10: 移植 TableForm/TableDesigner(表结构查看)

**参考源:** `~/dev/project/jookdb/cpp/tableform.*`、`sqlobject.h`(`Column`/`Index` 结构,已存在)、OpenDBKit 的 `tabledesignerdialog.*` 可作第二参考

- [ ] **Step 1:** 读 `sqlobject.h`(`Column`/`Index`/...,已在 jookdb 源中)与 `tableform.*`。
- [ ] **Step 2:** 用 `DESCRIBE_TABLE` 返回的列/主键填充结构视图(列名、类型、可空、主键标记)。
- [ ] **Step 3:** MVP 只读展示结构;结构修改通过在 `QueryForm` 执行 `ALTER`/`CREATE` 完成(不单做可视化 DDL 生成)。
- [ ] **Step 4:** 手动冒烟:选表 → 看到列与主键。
- [ ] **Step 5:** Commit:`feat(frontend): table structure view`

## Task 11: 移植 MainWindow + ConnDialog,组装完整应用

**参考源:** `~/dev/project/jookdb/cpp/mainwindow.{h,cpp}`(单例、菜单)、`conndialog.*`(`.cpp` 缺,需补写)

- [ ] **Step 1:** 读 `mainwindow.h`,移植单例 `MainWindow` 与菜单骨架,**剔除所有授权相关调用**(`GlobalUtils::checkProLicense`/`ActivateDialog` 等——本项目无授权)。
- [ ] **Step 2:** 补写 `ConnDialog`(`conndialog.ui` 在,`.cpp` 缺):MySQL/SQLite 两种连接表单 → 产出 `ConnData` → 调 `TEST_CONNECTION`/`OPEN_CONNECTION`。
- [ ] **Step 3:** 把 `BackendProcess`/`BackendClient` 提到 `MainWindow` 单例持有,供全局复用;退出时 `stop()`。
- [ ] **Step 4:** 用真正的 `MainWindow + ContentWidget` 替换 `MinimalWindow`,更新 `main.cpp`。
- [ ] **Step 5:** 实现 §5 的进程级错误处理:`BackendProcess` 退出监控 → 崩溃重启一次 → 再失败禁用 DB 操作并提示。
- [ ] **Step 6:** 端到端手动冒烟:启动 → 新建 SQLite/MySQL 连接 → 树浏览 → 查询 → 改数据 → 看结构。
- [ ] **Step 7:** Commit:`feat(frontend): full mainwindow + conn dialog, drop license code`

---

## Self-Review

**1. Spec 覆盖检查(对照设计文档,前端职责部分):**
- §3.1 前端拉起后端子进程 + 端口握手 → Task 4(BackendProcess)+ Task 6 串联 ✓
- §3.1 反向通道(WebSocket):MVP 的 6 个 funcId 均为请求/响应,无需反向回调;**设计中反向通道是为未来进度/确认预留,本 MVP 不实现**,在此明确记录(非遗漏)。
- §3.2 前端组件:BackendClient/BackendProcess/ConnData → Task 2/3/4 ✓;MainWindow/ContentWidget/MyTreeWidget/QueryForm/TableDataForm/TableForm/ConnDialog → Task 7–11 ✓
- §4 协议契约:ConnData.toOpenRequest + BackendClient 解析 ok/data/error → Task 2/3 ✓;FuncId 数值与后端一致 → Task 2 ✓
- §5 错误处理:通信级(TIMEOUT/TRANSPORT)→ Task 3;业务错误展示 → Task 6/8;进程级(崩溃重启)→ Task 11 Step 5 ✓
- §6 测试:ConnData 单测(T2)、集成测试(T5)、SqlLexer 单测(T8 Step 3);UI 以手动冒烟为主(符合设计"端到端脚本拉起前后端")✓
- §2 功能范围:连接管理(T11)、SQL 执行(T6/T8)、对象树(T7)、数据网格编辑(T9)、表结构查看(T10)✓

**2. 占位符扫描:** Phase A(T1–6)每个实现步骤含完整代码、每个测试步骤含完整测试与期望输出。Phase B(T7–11)是"先读指定源文件再适配"的任务,已点名具体参考文件与边界——这是有意的工程纪律,执行须知已说明,不是 TBD 式占位。✓

**3. 类型一致性核对:**
- `ConnData{connId,type,host,port,user,password,database,file}` + `toOpenRequest()` —— T2 定义,与后端 `ConnectionConfig` 字段对齐,T5/T6 使用一致 ✓
- `BackendClient::Result{ok,data,errorCode,errorMessage,transportFailed}` + `call(QJsonObject,int)` + `setPort(int)` —— T3 定义,T5/T6 使用一致 ✓
- `BackendProcess(QString jar)` + `start(int)/stop()/port()/isRunning()` —— T4 定义,T5/T6/T11 使用一致 ✓
- `FuncId::*` 数值 —— T2 定义,与后端 `FuncId.java`(后端计划 Task 2)逐一对应 ✓

**4. 跨计划一致性(前端 ↔ 后端):**
- 端口握手:后端 Main 打印 `JOOKKIT_PORT=<n>`(后端 T10)↔ 前端正则 `JOOKKIT_PORT=(\d+)`(T4)✓
- 响应壳 `{funcId,ok,data,error{code,message,sqlState}}`(后端 T2/T9)↔ 前端解析(T3)✓
- EXEC_SQL 响应 `{isQuery,columns,rows,affected}`(后端 T6)↔ 前端渲染(T6)✓
- 数据编辑请求 `{connId,table,values,pk}`(后端 T8)↔ 前端组装(T9)✓

无不一致。Phase A 完全可执行;Phase B 须按"先读后写"执行。
```
